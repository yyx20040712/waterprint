"""services 同层共享子包（同层晋升落点——星型：被同层消费，禁依赖消费方）。

输入:  无（包标记薄壳——件级独立 import，不立聚合正门）
输出:  无（单件子包，消费面直连模块路径）
"""

# ══════════════════════════════════════════════════════════════════
# 规格（《裁决书》方案二批 2a——B3-a 2026-09-19 同层晋升正门首件家）：
#   本子包=services 层内共享件家（跨层依赖铁律不动——共享件只被同层
#   消费，自身禁 import services/ 下任何消费方模块；ServiceContext
#   装配束例外经 services/__init__ 取——包根不回指子模块，无环）。
#   聚合正门不立（单件子包——消费面直连 latest_calc 模块路径；件多
#   再议 __init__ 聚合，先例=core registry/formulas 聚合正门）。
# ══════════════════════════════════════════════════════════════════
