"""路由包根：薄协议转换层（router 禁业务逻辑，≤150 行/文件）。

输入:  pydantic 请求模型
输出:  路由注册（main 装配）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结）
#
# 【五路由器】projects（CRUD）/ calc（计算与枚举任务）/ exports
#   （导出）/ events（SSE）/ scene（三维场景图——FE1 同步端点）——
#   各文件规格见各自头部。
# 【铁律】router 只做：请求校验（pydantic 自动）→ 调 service →
#   响应模型包装。出现 if/循环业务 = 拆去 service。
# ══════════════════════════════════════════════════════════════════
