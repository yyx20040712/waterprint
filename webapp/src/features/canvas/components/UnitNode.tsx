/**
 * 构筑物节点卡片：工程图例简笔轮廓 + 关键结果摘要一级可见（结构预留：随 M2 实装）。
 *
 * 输入:  单元 manifest 摘要 + 当前工况结果（字段 ID 取数）
 * 输出:  React Flow 节点渲染件
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 一级信息不下钻可见（§19.3）；端口颜色 = 流体语义色；
 *   - 同名构筑物跨线各自渲染参数（unit_id 唯一键）。
 */
