/**
 * React Flow 画布容器：节点/边渲染注册与视图控制（ADR-001）（结构预留：随 M2 实装）。
 *
 * 输入:  画布 store（节点/边/选中态）+ 单元清单（shared/api 生成客户端）
 * 输出:  工艺画布工作区（建图/连线/缩放/小地图）
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 节点 = 自包含 + 端口类型化；画布只管连接不管计算（Blender/n8n 契约借鉴）；
 *   - 50+ 节点不掉帧（§19.4）；节点摘要显示当前工况结果（工况切换不改图）；
 *   - 内置节点（市政输入/汇流/水质编辑）与单元节点同一渲染管线；
 *   - 画布状态全经 store（StrictMode 安全，§11 R14）。
 */
