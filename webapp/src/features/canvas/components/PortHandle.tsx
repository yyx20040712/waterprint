/**
 * 类型化端口：水蓝/泥棕/回流虚线的流体语义端口件（结构预留：随 M2 实装）。
 *
 * 输入:  端口声明（类型/方向/recycle 标记，来自 manifest）
 * 输出:  React Flow Handle 渲染件
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 连线规则与 core contracts/ports 校验同源：类型不匹配红色拒绝 + 原因 tooltip（§19.3）。
 */
