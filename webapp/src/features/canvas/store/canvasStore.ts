/**
 * 画布状态 slice：节点/边/选中态（zustand）（结构预留：随 M2 实装）。
 *
 * 输入:  用户编辑动作 + 项目加载（design/view 双态分离）
 * 输出:  画布 store（design 态编辑经保存流程，view 态本地持久化）
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - UI 态走 zustand；一切来自服务端的数据不进本 store（§17.2）；
 *   - 画布状态全经 store 持有，不依赖组件重挂载（§11 R14）。
 */
