/**
 * 方案浏览器表格：AntD Table virtual 虚拟滚动（千级行流畅）（结构预留：随 M2 实装）。
 *
 * 输入:  枚举结果分页（生成客户端，queryKey 含三元组）
 * 输出:  方案列表（裕度/成本列，语义色）
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 数字 tabular-nums 等宽对齐（§19.3）；裕度列正绿负红；
 *   - 枚举语义永远是单单元（ADR-005）；UI 不提供跨单元多选入口。
 */
