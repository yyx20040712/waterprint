/**
 * 高程纵断图：四线（水面/池底/管底/地面）+ 标高标注（ECharts）（结构预留：随 M2 实装）。
 *
 * 输入:  ElevationProfile 数据（core elevation/profile.py 产出）
 * 输出:  纵断可视化图表
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 前端零标高推算（纯展示投影）；横纵比例分设（比例参数来自后端 options）。
 */
