/**
 * R3F Canvas：灯光/地面/相机控制器（懒加载路由入口）（结构预留：随 M2 起（厂区总装 M5） 实装）。
 *
 * 输入:  场景图 JSON（core geometry/scene 产出）
 * 输出:  三维场景渲染容器
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 独立路由 + 懒加载 chunk（§12.6）；性能预算 1080p ≥60fps；
 *   - 前端零业务几何推导（§10.5/§16 A7）：渲染器只做类型化摆放。
 */
