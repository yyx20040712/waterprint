/**
 * 参数表单：按 manifest 字段 ID 渲染（单位灰阶小字显示）（结构预留：随 M2 实装）。
 *
 * 输入:  选中对象 manifest 参数定义 + 草稿值
 * 输出:  参数编辑表单（草稿→校验→提交）
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 错误消息来自 core（字段路径级），禁前端复制校验规则；
 *   - 输入单位换算在边界（core quantity.parse 语义）；显示单位可切换。
 */
