/**
 * 概算分级汇总表：分部分项/措施/间接/预备/税（可折叠行）（结构预留：随 M3 实装）。
 *
 * 输入:  EstimateSheet 数据（API，绑定 condition_key）
 * 输出:  分级汇总表（每笔溯源列：定额键/来源字段）
 *
 * 规格说明（骨架冻结，实装必须满足）：
 *   - 表列按字段 ID 取数（生成类型）；中文列名走 i18n 显示层；
 *   - 每笔金额可点开溯源（M4 任一数字可回溯的前端落点）。
 */
