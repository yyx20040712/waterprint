"""WaterPrint 计算内核包根：版本唯一真源与分层入口白名单。

输入:  无（包装配点）
输出:  __version__（引擎版本，可复算三元组成员之一，见 ADR-004）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结）
#
# 【分层】L0~L4 见 AGENTS.md §1 与 import-linter 契约；本 __init__ 禁止
#         import 任何子包（保持导入零副作用：Windows spawn 进程池前提，§12.2）。
#
# 【版本语义】__version__ 即 engine_version：结果 = f(design_hash,
#         engine_version, data_version) 三元组成员；版本变更意味着结果可能
#         变化，全部缓存与导出物按三元组失效（§12.3/§16 A8）。
#
# 【0.1.1】（批 13-D 收口，2026-09-08）patch 抬版=批 13 向量化实现形态
#         变更标记（13 数值单元同源向量路径唯一化——结果字节级恒等，
#         批 13-D 批报告注记）；锚材料版本字段全数 golden 冻结字面量
#         （零运行时 __version__ 读取点）对抬版不敏感；B12 缓存键材料
#         含 engine_version=抬版自然失效全部缓存，重放语义不感知。
#
# 【0.1.2】（批 14-FIX，2026-09-08）patch 抬版=两导出产物字节确定性
#         修复标记（calcbook xlsx docProps modified 载荷归一+DXF CLASSES
#         段记录序归一——产物内容语义零变，仅消除非内容噪声[渲染时钟/
#         ezdxf 注册序]；快照锚 ambr 三哈希恒不变——测试层规范化器
#         幂等吸收，批 14-FIX 批报告）；B12 缓存自然失效同 0.1.1 口径。
#
# 【公开名】仅暴露 __version__ 与 __all__；各子包经各自 __init__ 白名单访问。
# ══════════════════════════════════════════════════════════════════

__version__ = "0.1.2"
