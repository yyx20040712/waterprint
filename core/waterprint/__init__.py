"""WaterPrint 计算内核包根：版本唯一真源与分层入口白名单。

输入:  无（包装配点）
输出:  __version__（引擎版本，可复算三元组成员之一，见 ADR-004）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结）
#
# 【分层】L0~L4 见 AGENTS.md §1 与 import-linter 契约；本 __init__ 禁止
#         import 任何子包（保持导入零副作用：Windows spawn 进程池前提，§12.2）。
#
# 【版本语义】__version__ 即 engine_version：结果 = f(design_hash,
#         engine_version, data_version) 三元组成员；版本变更意味着结果可能
#         变化，全部缓存与导出物按三元组失效（§12.3/§16 A8）。
#
# 【公开名】仅暴露 __version__ 与 __all__；各子包经各自 __init__ 白名单访问。
# ══════════════════════════════════════════════════════════════════

__version__ = "0.1.0"
