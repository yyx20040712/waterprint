"""Excel 计算书渲染（模板驱动）：迹树+结果 → 数据注入模板（零写死逻辑）。

输入:  TraceTree + PlantResult + data/templates 计算书模板（.xlsx）
输出:  .xlsx 计算书（模板只做展示，全部数值由本文件写入）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；镜像测试 tests/trace/test_calcbook.py）
#
# 【公开接口】
#   render_calcbook(trace: TraceTree, result: PlantResult,
#                   template: Path, out: Path) -> Path
#   TEMPLATE_REGISTRY: Mapping[计算书章节键→模板文件与占位符映射]
#
# 【行为规格】
#   R1 模板与数据分离（§2 Excel 行）：模板是 data/templates 下版本化
#      .xlsx；本文件只做"占位符 → 值"注入，模板变更走版本化，
#      渲染代码不随版式变化——写盘代码里出现业务拼接 = 评审拒绝。
#   R2 模板禁公式（§11 R12）：加载模板时静态检查无公式单元格
#      （openpyxl 遍历），含公式模板 = 加载失败（计算单一事实源在
#      Python）。
#   R3 数值按字段 ID 注入（占位符语法 {{field_id}} 类），中文名只由
#      模板提供；每数值所在单元格可关联迹（批注或旁列公式 ID，
#      具体版式归模板设计）。
#   R4 确定性：同迹树+模板同输出字节（时钟取三元组）；输出快照
#      （syrupy 内容哈希）锁结构（§6.5——任何输出变化显式过审）。
#   R5 分册：单单元计算书（M1）→ 全厂计算书（M2 golden 验收）→
#      全量模板（M4）；分册选择是入参，渲染逻辑同一套。
#
# 【测试要求】注入完整性（占位符零残留断言）、公式模板拒绝、
#   确定性、快照回归、路径安全。
#
# 【参照】重写计划 §2/§6.5/§11 R12；data/templates/README.md
# ══════════════════════════════════════════════════════════════════
