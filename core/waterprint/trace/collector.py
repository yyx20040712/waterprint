"""计算迹收集：公式应用记录的采集与组织（全程可审计的数据半）。

输入:  registry.formulas.apply 的每次调用上下文（unit_id/工况/绑定值/输出）
输出:  迹树（按 工况→单元→公式 层级组织的 TraceNode 集合）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；镜像测试 tests/trace/test_collector.py）
#
# 【公开接口】
#   class TraceCollector（UnitContext 注入的协议对象）：
#       record(formula_id, bindings, output, norm_ref,
#              unit_id, condition_key) -> None
#       tree() -> TraceTree（不可变快照，含 repro 三元组）
#   collect(execution) -> TraceTree   执行期聚合正门
#
# 【行为规格】
#   R1 采集零遗漏：每条公式应用的输入快照（字段ID→规范单位值）、
#      输出、条文号、单元、工况全部入迹——漏记 = 审计断链
#      （§3 保证 5；测试断言迹节点数 == apply 调用数）。
#   R2 记录顺序确定性：同执行同迹序（按工况→拓扑层→单元→公式应用
#      序）；迹树序列化确定性（同结果双跑迹相同——diff=0 测试的
#      组成部分）。
#   R3 采集不改变语义：collector 故障不得吞掉计算结果？——否：
#      采集失败 = 执行失败（宁可失败不可无迹成功，审计完整性优先）。
#   R4 内存治理：万级枚举不逐方案入迹（采样策略：首/末/边界方案，
#      策略经 assumptions 配置）；全流程计算全量入迹。
#
# 【测试要求】节点数 == apply 数、确定性、采集失败传播、
#   枚举采样策略。
#
# 【参照】重写计划 §3-5/§3-6；借鉴 Dagster 运行可追溯（§15）
# ══════════════════════════════════════════════════════════════════
