"""旋流沉砂池计算实现：唯一计算源，向量化（标量 = N=1 特例，≤400 行）。

输入:  UnitContext（上游量 + 参数 + 工况 + 假设 + 迹收集器）
输出:  UnitResult（输出端口量 + 维度字段数组 + 警告 + 已用公式清单）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（结构预留：municipal_chenshachi，公式组随 M2 交付期冻结）
#
# 【公式组（旧实现交叉对照提示；依据以规范条文为准）】
#   表面负荷/停留时间、沉砂量
# 【编写规则】同 _template/compute.py：R1 公式经公式注册表求值；
#   R2 默认值经假设清单；R3 工况只经 manifest 映射后的参数；
#   R4 纯函数；R5 禁 import 其他单元与 L3 子系统；
#   R6 超 400 行按公式组拆子模块。
# ══════════════════════════════════════════════════════════════════
