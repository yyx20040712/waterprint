"""单元约束声明模板：声明式约束定义（键/限值/类型/知识库引用）。

输入:  本单元的工程约束（来自 constraint_kb 迁移条目 + 单元特有约束）
输出:  约束声明列表（solution/constraints.py DSL 表达）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；`wp new-unit` 生成的 constraints.py 必须满足）
#
# 【固定形态】
#   CONSTRAINTS: tuple[Constraint 声明, ...]   # 模块级唯一常量
#   每条：key / 表达式（受限 DSL：field_id 与常数比较 + AND）/
#   source（constraint_kb 键或规范条文）/ severity
#
# 【编写规则】
#   R1 限值有出处（规范条文或知识库条目键）；无出处约束不入列。
#   R2 表达式字段必须是 manifest 声明过的参数或 compute 产出的
#      结果字段 ID（引用失联 = 加载失败）。
#   R3 约束用于方案枚举过滤与结果校核两处（同一声明双消费，
#      禁止两处各写一份限值）。
# ══════════════════════════════════════════════════════════════════
