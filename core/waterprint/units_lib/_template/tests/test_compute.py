"""单元测试模板：golden 数值测试（对照 docs/norms 手算表，非自编数字）。

输入:  单元 compute 与 manifest；期望值来自 docs/norms 手算对照表
输出:  测试结果（先失败一次再通过——红-绿纪律）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；`wp new-unit` 生成的 test_compute.py 必须满足）
#
# 【固定形态】
#   - 每条公式至少 1 个 golden 数值断言（期望值来源：docs/norms
#     条文摘录 + 手算对照表，领域专家签字——禁止实现者自编数字，
#     §16 A9 测试盲区的唯一防线）；
#   - N=1 特例断言：单点 compute 结果 == 枚举网格 N=1 行（防双轨）；
#   - 纯函数断言：同 ctx 双跑结果相同；
#   - trace 断言：UnitResult.formula_ids 非空且全部可在公式注册表
#     解析（注册表与实现漂移防线，§16 A1）。
#
# 【锁定流程】本文件写完并由人类复核后执行
#   `python scripts/lock_tests.py core/waterprint/units_lib/<line>/<unit>/tests`
#   转为只读（AGENTS.md §11）。
# ══════════════════════════════════════════════════════════════════
