"""单元清单声明模板：参数/端口/去除率/规范引用/工况映射（声明式唯一真源）。

输入:  本单元的工程定义（参数范围、端口、系数引用、条文出处）
输出:  UnitManifest 实例（load_manifest 静态校验通过才算合法）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；`wp new-unit` 生成的 manifest.py 必须满足）
#
# 【固定形态】
#   UNIT_ID = "<line>_<name>"          # 全库唯一、稳定（进项目文件）
#   manifest = load_manifest({...})    # 模块级唯一实例
#   声明内容五件：params（含离散网格）/ ports（含 recycle 标记）/
#   removal_refs（coefficients 键）/ norm_refs（条文号列表，非空）/
#   condition_mappings（n_active 等声明式规则）
#
# 【编写规则】
#   R1 参数逐条复核：范围/默认值必须有出处（旧 mod.json 仅作参考，
#      依据 = 规范条文，§5 迁移原则）；
#   R2 去除率只存引用键（数值在 data/coefficients）；
#   R3 工况映射禁止过程逻辑（受限 DSL，manifest.py 契约 R1c）；
#   R4 同名构筑物跨线各写各的清单，禁止复制参数暗耦合（§14.3）。
# ══════════════════════════════════════════════════════════════════
