"""单元包模板：脚手架生成目标形态的规范样本（本包不注册为可用单元）。

输入:  无（模板）
输出:  暴露 manifest 与 compute 两个名字（单元包唯一对外白名单）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结）
#
# 【模板用途】`wp new-unit <line> <name>`（cli.py）复制本包为新单元并
#   替换标识；结构一致性不靠文档靠工具（§15 工程细节 6）。
# 【白名单】真实单元包 __init__ 只导出：manifest（UnitManifest 实例）
#   与 compute（Unit 协议实现工厂）；多导出一个名字 = 评审拒绝。
# 【排除】模板包本身带 unit_id="__template__"，discover_units 跳过。
# ══════════════════════════════════════════════════════════════════
