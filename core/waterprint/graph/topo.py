"""拓扑排序 + 强连通分量（SCC）划分（纯函数，回路一等公民的第一步）。

输入:  节点列表 + 边列表（含 recycle 标记）
输出:  执行分层（DAG 部分按层排序）+ 回路组（SCC 列表，供 loop.py 迭代）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；镜像测试 tests/graph/test_topo.py + properties_topo.py）
#
# 【公开接口】
#   topological_layers(nodes, edges) -> tuple[tuple[str, ...], ...]
#       DAG 部分的分层执行序（同层可并行；层序确定性：按 unit_id 稳定排序）
#   strongly_connected_components(nodes, edges) -> tuple[tuple[str, ...], ...]
#       回路组（Tarjan 或等价算法；组内节点互相可达）
#   split_graph(nodes, edges) -> (layers, loop_groups)
#       总入口：一次划分出"无环分层 + 回路组"
#
# 【行为规格】
#   R1 环不是错误（§3 保证 3，病灶"DAG 环路直接异常、污泥回流绕行"）：
#      recycle 边构成的环被划入 SCC 组交给 loop.py；只有"非 recycle 边
#      构成的环"（用户误连）才抛 InvalidConnection（附环上的节点路径）。
#   R2 纯函数：无副作用、无内部可变状态；同输入同输出（可复算前提）。
#   R3 层序确定性：同层节点按 unit_id 字典序——结果与保存/加载顺序无关。
#   R4 孤立节点/悬空边：孤立节点进第 0 层；悬空边（端点缺失）抛领域异常。
#
# 【测试要求】已知图分层正确、recycle 环进 SCC 而非异常、误连环异常
#   （消息含路径）、确定性（节点乱序输入同层序）；性质：任意 DAG 分层后
#   每条边都从低层指向高层、SCC 内任意两点互达（hypothesis）。
#
# 【参照】重写计划 §3-3/§14.2；ADR-003
# ══════════════════════════════════════════════════════════════════
