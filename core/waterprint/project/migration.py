"""format_version 迁移链：旧版项目 JSON → 当前版对象（链式、可测试）。

输入:  任意历史 format_version 的项目 JSON
输出:  当前版 ProjectFile（迁移路径记录进 metadata.migrated_from）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；镜像测试 tests/project/test_migration.py）
#
# 【公开接口】
#   SUPPORTED_VERSIONS: tuple[str, ...]    迁移链覆盖的版本序列
#   migrate(data: Mapping) -> ProjectFile  自动识别版本逐级迁移
#
# 【行为规格】
#   R1 链式迁移：v(n)→v(n+1) 每步一个纯函数迁移器，注册进链；
#      任意旧版经链到达当前版；跳级 = 链式复合，禁止写 n→current
#      的快捷迁移（组合爆炸与漏网）。
#   R2 迁移器纯函数 + 显式记录：每步迁移写入迁移日志
#      （字段增删改列表），进 metadata（审计可见）；不可迁移的
#      字段（语义不明）抛领域异常并指明字段路径——禁止猜测性默认。
#   R3 未来版本（format_version > 当前）→ 明确拒绝（不降级打开，
#      防静默丢数据）。
#   R4 每个迁移器配 golden 用例：旧版样本 → 迁移后断言
#      （样本文件进 core/tests/golden/golden_data/migrations/，
#      由人类维护）。
#   R5 M4 旧系统导入器（best-effort）是独立入口（app.py 编排），
#      不混入本迁移链（旧格式非本产品版本史）。
#
# 【测试要求】链式到达、逐步日志、不可迁移拒绝、未来版拒绝、
#   golden 样本往返。
#
# 【参照】重写计划 §7 M4/§12.3
# ══════════════════════════════════════════════════════════════════
