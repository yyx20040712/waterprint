"""向量化批量计算：网格 × 上游上下文 → 结果 DataFrame（万级枚举 <5s 的实现所）。

输入:  Grid + 上游 UnitContext 快照（固定）+ 单元 compute（向量化唯一实现）
输出:  结果 DataFrame（每行一个方案：参数列 + 结果维度字段列 + 约束列）
"""

# ══════════════════════════════════════════════════════════════════
# 规格说明（骨架冻结；镜像测试 tests/solution/test_enumerate.py）
#
# 【公开接口】
#   enumerate_solutions(grid: Grid, upstream: UnitContext,
#                       unit: Unit, env: RunEnv) -> pandas.DataFrame
#
# 【行为规格】
#   R1 单实现双用（§3 保证 1）：枚举调用与单点计算走**同一个向量化
#      compute**（网格行批量喂入），禁止"逐方案循环调标量"的退化和
#      "另写一套批量公式"的双轨——性能测试守卫（万级 <5s，§18.1）。
#   R2 物理不变性（性质测试常驻）：结果维度字段非负；有效容积类字段
#      随池数/尺寸参数单调（在网格有序维上断言）。
#   R3 上游上下文冻结：枚举期间上游量不变（快照传入）；工况取当前
#      选定档（枚举结果标注 condition_key）。
#   R4 输出形态：DataFrame 列名 = 字段 ID（dimensions 注册），附
#      param 列与 margin 预备列；排序/分页在 ranking/服务层做，
#      本文件不做截断（§12.2 分页默认 200 条在服务层）。
#   R5 NaN 政策：约束外推导致的 NaN 不允许静默通过——显式标注列，
#      下游过滤时计数报告。
#
# 【测试要求】N=1 网格结果 == 单点 compute（防双轨）、结果行数 == total、
#   非负性/单调性性质、condition_key 标注。
#
# 【参照】重写计划 §3-1/§12.4/§18.1；ADR-005
# ══════════════════════════════════════════════════════════════════
