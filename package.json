{
  "name": "waterprint",
  "private": true,
  "version": "0.0.1",
  "description": "智水蓝图 monorepo：core(Python) + server(FastAPI) + webapp(React)",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm -C webapp dev",
    "build": "pnpm -C webapp build",
    "test": "pnpm -C webapp test",
    "gates": "python scripts/run_gates.py"
  }
}
